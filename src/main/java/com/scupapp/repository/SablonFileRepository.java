package com.scupapp.repository;

import com.scupapp.entity.SablonFile;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SablonFileRepository extends JpaRepository<SablonFile, Long> {
}
